{
  "name": "simple-task-tracker-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port ${PORT:-3001}",
    "dev": "npm run start"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
